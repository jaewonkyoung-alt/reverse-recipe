# 리버스레시피 마스터 플랜

> 작성일: 2026-09-23
> 최종 목표: **Google Play 스토어 출시**
> BM: **하단 배너 광고**
>
> 관련 문서 — 기술 설계는 `REBUILD_PLAN.md`, 출시 체크리스트는 `RELEASE.md`,
> 작업 큐는 `BACKLOG.md`, 사람 결정은 `DECISIONS.md`, 자동화는 `.autoteam/`

---

## 0. 이 계획의 두 가지 핵심 판단

### 판단 ①  BM이 플랫폼 결정을 대신 내려줬다

배너 광고를 넣기로 한 순간, 출시 경로 선택지가 하나로 줄어든다.

```
배너 광고
  → AdSense 는 앱에 못 쓴다 (정책상 금지, AdMob 만 예외)
  → AdMob 필요
  → AdMob 은 네이티브 SDK (Google Mobile Ads SDK)
  → 배너를 놓을 네이티브 표면이 필요하다
  → TWA 는 화면 전체가 웹페이지라 놓을 자리가 없다  ✗
  → Capacitor / 네이티브  ✓
```

AdMob 정책은 **"Google Mobile Ads SDK를 쓰는 한 WebView 옆에 광고를 띄우는 것은 허용"** 이라고 명시한다.
Capacitor가 정확히 이 형태다 — 네이티브 Activity 안에 WebView를 띄우고, 그 아래에 배너를 붙인다.

→ **`DECISIONS.md` #1 은 B(Capacitor)로 확정.** 기술 판단이 아니라 BM이 결정했다.

### 판단 ②  전면 재설계를 마친 뒤에 출시하면 안 된다

`REBUILD_PLAN.md`의 기술 재설계(온톨로지·임베딩·RAG)는 몇 달짜리다.
반면 출시에는 **되돌릴 수 없는 대기 시간**이 박혀 있다.

> 개인 개발자 계정은 프로덕션 신청 전에
> **테스터 12명이 14일 연속 opt-in** 상태인 비공개 테스트를 거쳐야 한다.

이 14일은 **빌드가 나와야 비로소 시작되는 시계**다.
재설계를 먼저 끝내면 그 몇 달 뒤에야 시계가 돌기 시작한다.

**→ 순서를 뒤집는다. 최소한으로 정직한 앱을 먼저 올려 시계를 돌리고,
그 14일 동안 재설계를 병행한다.**

---

## 1. 단계별 계획

### Phase 0 — 거짓말 제거 (9/23 ~ 9/30, 1주)

출시 심사에서 **"앱 설명과 실제 기능이 다름"은 명확한 반려 사유**다.
현재 앱에는 사용자에게 사실이 아닌 것을 보여주는 지점이 넷 있다. 이것부터 없앤다.

| ID | 문제 | 위치 |
|----|------|------|
| B-010 | **AI 추천이 죽어있는데 살아있는 척한다.** Gemini 키가 무효(400)인데 `console.error`만 남기고 Mock 11종으로 조용히 대체. 기동 배너는 키 존재만 보고 "Connected" 출력 | `recipeService.ts:393`, `index.ts:107` |
| B-002 | 재료 매칭이 조용히 틀린다. `무염버터`→`무`(14일), `파김치`→`파`(7일) | 4곳 |
| B-003 | 소비기한 DB가 이중화돼 이미 갈라짐 | backend/frontend |
| B-005 | 해시로 만든 가짜 가격·칼로리를 실제 정보처럼 표시 | `shopping.ts:150`, `RecipeCard.tsx:26` |
| B-006 | 홈 화면만 "Perplexity AI 기반" (백엔드는 Gemini) | `HomePage.tsx:92` |

**B-010이 최우선이다.** 앱의 핵심 기능이 지금 이 순간 작동하지 않고, 아무도 모르게 되어 있다.

산출물: 새 Gemini API 키, 실패 시 사용자에게 보이는 명시적 상태 표시.

### Phase 1 — 출시 가능한 형태로 (10/1 ~ 10/21, 3주)

| 항목 | 내용 | 담당 |
|------|------|------|
| 백엔드 호스팅 | SQLite 파일 DB라 **영속 볼륨 필요**. 서버리스로 가면 DB 교체가 따라온다 | 사람 |
| Capacitor 래핑 | 프론트 번들 + 네이티브 셸 | 프로그래머 |
| 타깃 API 36 | 2026-08-31부터 필수. **이미 마감이 지났다** | 프로그래머 |
| AdMob 배너 | §2 설계대로 | 프로그래머 |
| 개인정보처리방침 | **광고 데이터 수집 포함해서** 작성, 공개 HTTPS 게시 | 재무부 + 사람 |
| 데이터 안전 양식 | 광고 ID 수집 선언 | 재무부 |
| AD_ID 권한 | API 33+ 는 `com.google.android.gms.permission.AD_ID` 매니페스트 선언 필수 | 프로그래머 |
| UMP 동의 | EU 사용자 동의 정책. 국내만 받아도 구조는 넣어둘 것 | 프로그래머 |
| 스토어 자산 | 아이콘 512, 피처 그래픽 1024x500, 스크린샷 | 재무부 + 사람 |

### Phase 2 — 비공개 테스트 (10/22 ~, 최소 14일)

- 테스터 12명 모집 → **14일 연속 opt-in 유지**
- 이 기간에 이탈자가 나오면 시계가 리셋될 수 있으므로 여유 있게 15~16명 확보
- **이 14일은 대기 시간이 아니다. Phase 3을 병행한다.**

### Phase 3 — 기술 재설계 (Phase 2와 병행)

`REBUILD_PLAN.md` Phase 1~2를 그대로 수행한다.

- pg 호환 shim 제거 → Kysely
- 식재료 온톨로지 구축 (3단 정규화)
- 공공데이터 레시피 코퍼스 적재 (공공데이터포털 15057205)
- 점수 공식 교체 (평균 → 총량, IDF 가중)
- 캐시 분리 (검색은 캐시, 랭킹은 매번)
- 식약처 영양 API로 가짜 칼로리 대체

테스터 피드백이 이 단계의 우선순위를 조정한다.

### Phase 4 — 프로덕션 (11월 중)

- 프로덕션 액세스 신청
- 단계적 출시 (10% → 50% → 100%)
- 광고 수익·크래시율 모니터링

---

## 2. 광고 배치 설계

### 2.1 레이아웃 문제를 먼저 인정할 것

현재 `Layout.tsx`에 **하단 탭 내비게이션이 이미 있다.** 여기에 배너를 더하면 하단이 쌓인다.

```
┌─────────────────┐
│                 │
│    컨텐츠        │
│                 │
├─────────────────┤
│ 하단 내비 ~56dp  │
├─────────────────┤
│ 배너 50dp       │  ← 추가됨
├─────────────────┤
│ safe area ~34dp │
└─────────────────┘
   하단 합계 ~140dp
```

작은 화면(667dp)에서 **세로의 약 21%** 를 하단이 먹는다. 무시할 수준이 아니다.

### 2.2 배치 원칙

| 원칙 | 이유 |
|------|------|
| 배너는 하단 내비 **아래**, 화면 최하단 | 내비 위에 두면 탭 누르다 광고를 오탭한다. AdMob 정책상 오탭 유발 배치는 금지 |
| **조리 모드에서는 배너를 숨긴다** | 타이머를 가리고, 젖은 손으로 오탭하기 쉽다. UX·정책 양쪽에서 나쁘다 |
| Adaptive Banner 사용 | 고정 50dp보다 기기별 최적 높이로 잡힌다 |
| 레시피 상세 스크롤 중 고정 | 인라인 삽입은 조리 단계를 끊는다 |
| 빈 상태 화면에는 표시 | 냉장고가 비었을 때는 컨텐츠 경쟁이 없다 |

### 2.3 수익에 대한 정직한 기대치

배너 광고는 **DAU가 붙기 전까지 의미 있는 수익이 아니다.**
국내 레시피 앱 배너 eCPM과 초기 DAU를 감안하면 월 단위로 커피값 수준이다.

그래도 지금 넣는 이유는 둘이다.
1. 나중에 넣으면 레이아웃을 다시 짜야 한다 — 하단 140dp는 설계 초기에 반영해야 한다
2. 광고 ID 수집은 개인정보처리방침·데이터 안전 양식에 영향을 준다. 출시 후 추가하면 **재심사 대상**이다

→ 수익이 아니라 **구조를 미리 잡는 것**이 목적이다.

---

## 3. 자동화가 맡는 부분

```
프로그래머 (Claude Code)  → Phase 0, 1, 3 의 코드 작업
평가자    (Grok 1.0.40)  → 매 라운드 검수, PASS 여부 판정
재무부    (Gemini)       → 정책 문서, 스토어 자산, RELEASE.md 관리
```

```bash
./.autoteam/run.sh --run dev       # 프로그래머 ↔ 평가자
./.autoteam/run.sh --run release   # 재무부 ↔ 평가자
```

PASS 시에만 `autoteam/<날짜>` 브랜치로 푸시하고 PR을 연다. main은 건드리지 않는다.

**사람이 직접 해야 하는 것** (봇이 대신 못 함)

- [ ] Play Console 계정 + $25
- [ ] 새 Gemini API 키 발급 (B-010 + 재무부 봇이 동시에 막혀 있음)
- [ ] AdMob 계정 개설 및 앱/광고 단위 등록
- [ ] 백엔드 호스팅 결정 및 결제
- [ ] 개인정보처리방침 HTTPS 게시
- [ ] 콘텐츠 등급 설문
- [ ] 앱 서명 키 (Play App Signing)
- [ ] 테스터 12명 모집 및 14일 유지
- [ ] AAB 업로드

---

## 4. 경로상의 큰 위험

| 위험 | 영향 | 대응 |
|------|------|------|
| **14일 시계가 늦게 시작** | 출시 전체가 밀린다 | Phase 0·1을 최소 범위로 끊고 빌드를 먼저 낸다 |
| 테스터 12명 미달·이탈 | 시계 리셋 | 15~16명 확보. 주변부터 |
| 백엔드 호스팅 미정 | Phase 1 전체 차단 | **이번 주에 결정.** 가장 앞선 블로커 |
| SQLite ↔ 서버리스 충돌 | DB 교체가 재설계와 얽힘 | 영속 볼륨 있는 호스팅을 고르면 회피 가능 |
| 광고 레이아웃 후반 삽입 | 재설계 + 재심사 | Phase 1에 반드시 포함 |
| 공공 레시피 코퍼스 품질 미달 | Phase 3 설계 흔들림 | Phase 3 착수 전 표본 검증 |

---

## 5. 이번 주에 할 일

1. **새 Gemini API 키 발급** → `backend/.env` 교체
   B-010(앱 AI 추천 복구)과 재무부 봇이 동시에 풀린다
2. **백엔드 호스팅 결정** (`DECISIONS.md` #2) — 가장 앞선 블로커
3. `./.autoteam/run.sh --run dev` 로 Phase 0 착수 (B-002부터)
4. AdMob 계정 개설

---

## 6. 출처

- [AdSense 프로그램 정책 — AdMob 고객센터](https://support.google.com/admob/answer/48182?hl=en) (앱 내 AdSense 금지, AdMob 예외)
- [AdMob 정책 및 제한사항](https://support.google.com/admob/answer/6128543?hl=ko)
- [광고 ID — Play Console 고객센터](https://support.google.com/googleplay/android-developer/answer/6048248?hl=ko)
- [EU 사용자 동의 정책 준수하기 — AdMob](https://support.google.com/admob/answer/7666519?hl=ko)
- [새로운 개인 개발자 계정의 앱 테스트 요구사항](https://support.google.com/googleplay/android-developer/answer/14151465?hl=ko)
- [Target API level requirements for Google Play apps](https://support.google.com/googleplay/android-developer/answer/11926878?hl=en)
